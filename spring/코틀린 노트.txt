<>쓰고나서 =이 나오면 무조건! <> = 이렇게 한칸띄워야함

기본적으로 jpa 엔티티는,필수로 입력받는건 주생성자(클래스옆에생성자)에 넣고,부가적으로 입력받는건 필드로 사용

jpa엔티티에서 id는 부가적으로 받는 필드에 널허용으로 넣자

엔티티에서 id는,GeneratedValue쓰려면 널허용해야함

코틀린에선 주생성자가 있으면 부생성자에서도 그 주생성자를 무조건 호출해야하기때문에,부생성자는 주생성자에서 추가로 뭐 받을거있을때 사용하면되고
가능하면 주생성자에서 디폴트값을 주는거로 해결할수있음

EntityToDto를 만들땐,dto에서 함수확장을 사용해서,엔티티쪽에 함수를 추가하는느낌으로 하는게 나은듯

데이터클래스에서 자동으로 추가해주는 toString같은것들은,주생성자꺼만 들어있으니 주의

코틀린에서 abc.class하려면
	AuditingEntityListener::class
이렇게하면됨 콜론2개

코틀린에서 롬복없이 slf4j부르려면
	private val log=LoggerFactory.getLogger(this.javaClass)
