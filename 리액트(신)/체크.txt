props로 이벤트넘겨줄땐 대소문자까지해서 이름 완전히 똑같이해서줘야함

체크할때는 map써서 원하는값일떄만 동작하게해서 그거만바꾸는식도있는데 이건 코스트많이들듯

밑에 컨포넌트에서 위로 올려줄땐 위에서 클릭함수같은거만들어서 
매개변수 넣어서 setstate하는식으로 만든다음에 함수를 props으로 보내면 밑에서 그함수를 쓰면 위에 setstate해서 위에 들어감 