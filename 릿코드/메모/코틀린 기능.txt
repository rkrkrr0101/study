
val cList= MutableList(size){value}//저사이즈만큼 밸류로 채운 리스트 반환,선언을 mutableList로하거나 List로 하는식으로 변형여부바꾸면됨
이때 이중리스트인경우엔
이경우엔
val busRouteList = MutableList(maxStation+1){ mutableSetOf<Int>() }
이렇게해도 얕은복사안남
Collections.nCopies(size, value) 이건 사용금지,이상하게나옴

routes.map{it.toList()}.toList() // Array<IntArray>를 리스트로 변환하는방법

val maxStation:Int=routeList.flatten().max()//이중리스트에서 최대값찾기